
services:
  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file: # Mówię Compose, żeby wczytał zmienne z tego pliku.
      - .env
    ports:
      - "5432:5432"

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./src:/app/src
    ports:
      - "8000:8000"
    env_file: 
      - .env
    environment:
      - POSTGRES_HOST=db 
    depends_on:
      - db

volumes:
  postgres_data: